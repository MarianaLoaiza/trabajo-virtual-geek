const name = 'Mariana';
const sizeName = name.length;

const titulo = document.querySelector('.title');

titulo.innerHTML = `El nombre de mi compañera es ${name}, y está compuesto por ${sizeName} caracteres`;