const btn = document.getElementById('type');

btn.addEventListener('click', (e) =>{
    console.log(e)
})

// la e representa un objeto de tipo evento, type es un 
// atributo que nos permite saber de que tipo de 
// evento estamos hablando cuenado accedemos a la funcion 
